{
  "name": "meow_media",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",

    "build": "npm install --prefix server && npm install --prefix client && npm exec --prefix client vite build",

    "start": "node server/app.js",

    "dev": "concurrently \"npm run dev --prefix client\" \"npm run start --prefix server\""
  },
  "devDependencies": {
    "concurrently": "^8.2.1"
  }
}
